/**
 * Stable unique calendar identifier for this app (map key in {@link CalendarsMap}). Not the
 * resource URL — use {@link Calendar.url} on {@link Calendar} for that.
 */
export type CalendarId = string;
