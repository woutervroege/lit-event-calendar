/**
 * Calendar collection resource URL (e.g. CalDAV href). Not unique globally: the same path can
 * exist under different {@link CalendarAccountId | accounts}.
 */
export type CalendarUrl = string;
